# Database
MONGODB_URI=mongodb://localhost:27017/ecommerce
MONGODB_URI_PROD=mongodb+srv://vm614168_db_user:<db_password>@cluster0.agjktdq.mongodb.net/ecommerce?retryWrites=true&w=majority

# JWT
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRE=7d

# Razorpay
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_secret
RESET_PASSWORD_EXPIRE_MINUTES=15
OTP_EXPIRE_MINUTES=10

# SMTP (for OTP emails)
SMTP_HOST=smtp.zoho.in
SMTP_PORT=465
SMTP_SECURE=SSL
SMTP_USER=bharathorganic@zohomail.in
SMTP_PASS=csH6Y9R5twsD
EMAIL_FROM=bharathorganic@zohomail.in
SMTP_CONNECTION_TIMEOUT_MS=10000
SMTP_GREETING_TIMEOUT_MS=10000
SMTP_SOCKET_TIMEOUT_MS=20000

# Server
PORT=5000
NODE_ENV=development
AUTO_SEED_ON_DEPLOY=false
# When true: backend checks SeedState on startup and seeds only once (first deploy).

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000,https://your-streamlit-app.streamlit.app

# Seed/Admin bootstrap
ADMIN_NAME=Admin User
ADMIN_EMAIL=admin@shopease.com
ADMIN_PASSWORD=admin123
ADMIN_PHONE=9876543210


